/*'use strict';

describe('Service: salesPlan', function () {

  // load the service's module
  beforeEach(module('crmPostgresWebApp'));

  // instantiate service
  var salesPlan;
  beforeEach(inject(function (_salesPlan_) {
    salesPlan = _salesPlan_;
  }));

  it('should do something', function () {
    expect(!!salesPlan).toBe(true);
  });

});*/
