/*
'use strict';

describe('Service: peopleCompanies', function () {

  // load the service's module
  beforeEach(module('crmPostgresWebApp'));

  // instantiate service
  var peopleCompanies;
  beforeEach(inject(function (_peopleCompanies_) {
    peopleCompanies = _peopleCompanies_;
  }));

  it('should do something', function () {
    expect(!!peopleCompanies).toBe(true);
  });

});
*/
