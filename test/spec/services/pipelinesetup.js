/*
'use strict';

describe('Service: PipelineSetup', function () {

  // load the service's module
  beforeEach(module('crmPostgresWebApp'));

  // instantiate service
  var PipelineSetup;
  beforeEach(inject(function (_PipelineSetup_) {
    PipelineSetup = _PipelineSetup_;
  }));

  it('should do something', function () {
    expect(!!PipelineSetup).toBe(true);
  });

});
*/
