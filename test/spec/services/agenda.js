'use strict';

describe('Service: agenda', function () {

  // load the service's module
  beforeEach(module('crmPostgresWebApp'));

  // instantiate service
  var agenda;
  beforeEach(inject(function (AgendaService) {
    agenda = AgendaService;
  }));

  it('should do something', function () {
    //
  });

});
